import React, {Component} from 'react';
import PackageList from '../ui/create/PackageList';
import InputWrapper from '../ui/create/InputWrapper';

export default function Create(props) {
        return (
          <div>
            <PackageList/>
          </div>
        );
    }
